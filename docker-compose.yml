services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    environment:
      - PLEX_URL=${PLEX_URL}
      - PLEX_TOKEN=${PLEX_TOKEN}

  ui:
    build: ./ui
    ports:
      - "3000:80"
    depends_on:
      - api
